export interface IProduct {
    id: number,
    name: string,
    description: string,
    interestRate: number,
    minMonth: number,
    maxMonth: number,
    minAmount: number,
    maxAmount: number
}